import { NextRequest, NextResponse } from 'next/server';
import { getAiReview, getEvaluation, listAgents, listCategoryScores, listFindings, saveAiRequest, saveAiResponse } from '@/lib/db';
import { createManualAiRequest, validateManualAiResponse } from '@/lib/manual-ai';
import { ZodError } from 'zod';
export const runtime='nodejs';export const dynamic='force-dynamic';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params,evaluation=getEvaluation(id);if(!evaluation)return NextResponse.json({error:'Evaluation not found'},{status:404});if(evaluation.status!=='completed')return NextResponse.json({error:'Deterministic evaluation must complete first'},{status:409});let review=getAiReview(id);if(!review){const request=createManualAiRequest(evaluation,listAgents(id),listCategoryScores(id),listFindings(id));saveAiRequest(id,request);review=getAiReview(id);}return NextResponse.json(review);}
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params,evaluation=getEvaluation(id);if(!evaluation)return NextResponse.json({error:'Evaluation not found'},{status:404});try{const input=await req.json();const response=validateManualAiResponse(input,evaluation,listFindings(id));if(!getAiReview(id))saveAiRequest(id,createManualAiRequest(evaluation,listAgents(id),listCategoryScores(id),listFindings(id)));saveAiResponse(id,response);return NextResponse.json({response});}catch(error){const message=error instanceof ZodError?error.issues.map(i=>`${i.path.join('.')}: ${i.message}`).join('; '):error instanceof Error?error.message:'Invalid JSON response';return NextResponse.json({error:message},{status:400});}}
